export interface Options {
  hostKey?: string;
  portKey?: string;
  host?: string;
  port?: number;
  silent?: boolean;
  name?: string;
  bodyLimit?: number;
  caseSensitive?: boolean;
  requestIdHeader?: string;
}
