{
  "name": "revane-fastify",
  "version": "3.1.1",
  "description": "",
  "main": "bin/RevaneFastify.js",
  "scripts": {
    "unit": "tap test/*.test.js",
    "test": "npm run compile && npm run lint && npm run unit",
    "lint": "tslint --project tsconfig.json --config tslint.yaml",
    "coveralls": "npm run compile && npm run unit --  --cov",
    "coverage": "npm run coveralls && tap --coverage-report=lcov",
    "compile": "tsc -d",
    "prepublishOnly": "npm run compile -s"
  },
  "author": "Denis Fäcke",
  "license": "MIT",
  "engines": {
    "node": ">= 8.0.0"
  },
  "devDependencies": {
    "@types/node": "^11.11.3",
    "coveralls": "^3.0.2",
    "request": "^2.87.0",
    "standard": "^12.0.1",
    "tap": "^12.0.1",
    "tslint": "^5.14.0",
    "tslint-config-standard": "^8.0.1",
    "typescript": "^3.3.3333"
  },
  "dependencies": {
    "fastify": "^2.1.0",
    "fastify-plugin": "^1.5.0"
  },
  "keywords": [
    "inversion of control",
    "ioc",
    "dependency injection",
    "fastify"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/SerayaEryn/revane-fastify.git"
  },
  "typings": "bin/RevaneFastify.d.ts"
}
